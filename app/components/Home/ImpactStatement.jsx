import React from 'react'

const styles = {
  impactStatement: {
    color: 'white',
    marginTop: 50,
    marginBottom: 50
  }
};

const ImpactStatement = props => (
  <div style={ styles.impactStatement }>
    Putting the ‘peer’ back into peer review.
  </div>
)

export default ImpactStatement
